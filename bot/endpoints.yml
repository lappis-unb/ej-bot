tracker_store:
  type: SQL
  dialect: "postgresql"
  url: ${DB_HOST}
  db: ${DB_DATABASE}
  username: ${DB_USER} 
  password: ${DB_PASSWORD}
  port: ${DB_PORT}
lock_store:
  type: redis
  url: ${REDIS_HOST}
  port: ${REDIS_PORT}
action_endpoint:
  url: ${ACTIONS_SERVER_URL}

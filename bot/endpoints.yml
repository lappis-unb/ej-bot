tracker_store:
  type: SQL
  dialect: "postgresql"
  url: ${POSTGRES_HOST}
  db: ${POSTGRES_DB}
  username: ${POSTGRES_USER} 
  password: ${POSTGRES_PASSWORD}
lock_store:
  type: redis
  url: ${REDIS_HOST}
  port: ${REDIS_PORT}
action_endpoint:
  url: ${ACTIONS_SERVER_URL}
